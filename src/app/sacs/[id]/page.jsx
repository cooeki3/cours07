// import cards from...

 const  = async ({params}) => {
        const {id} = await params; // correspond au nom du dossier dynamique        

        return (
            <div>
                {/* Affiche Catégorie: culture */}
                <h1>Catégorie: {categoryId}</h1> 
            </div>
        );
    };